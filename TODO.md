# TODO

## 优先级排序

### P0 — 当前迭代

- [ ] **修复 B-001**：下载完成后页面不停刷新的回归 bug  
  *排查方向*：Polling timer 未在任务结束后清除，或 `loadJobs()` 逻辑有问题

- [ ] **修复 B-002**：B站下载视频无法播放  
  *方向 A*：尝试添加 `--fixup` 参数强制修复  
  *方向 B*：对 B站换用 `-f bestvideo+bestaudio` 而非分段合并  
  *方向 C*：确认是否是预览播放器问题（本地 VLC 能播则只是前端预览问题）

- [ ] **修复 B-004**：URL 过长导致任务卡片布局破窗  
  *修复方案*：`word-break: break-all` + `max-width` 限制，配合标题截断

### P1 — 下一迭代

- [ ] **修复 B-003**：iPad 上粘贴剪贴板无效  
  *方案*：检测 iOS Safari，fallback 到使用 `<input>` 的 clipboard API 或引导用户手动粘贴

- [ ] **历史记录管理**：增加“清空历史”按钮，并限制历史最多只保留最后 5 条

- [ ] **历史下载预览播放器增强**：支持 mkv/webm 的 `<video>` 直接播放（当前依赖系统编解码器）

- [ ] **多任务并发下载**：当前同一 yt-dlp 实例只能串行执行；可考虑 `concurrent` 参数或任务队列

### P2 — 未来

- [ ] **移动端适配**：触摸友好的大按钮、简化布局选项

- [ ] **下载限速 UI 完善**：支持 KB/s 级别精细限速

- [ ] **自定义保存路径**：允许用户指定下载目录（当前写死 Downloads 子目录）

- [ ] **导出/导入下载历史**：JSON 备份，防止 history.json 丢失

- [ ] **Cookie 管理**：支持从浏览器导出 cookie 文件上传（针对需要登录的视频）

- [ ] **视频封面缩略图预览**：在历史列表里直接显示视频封面
